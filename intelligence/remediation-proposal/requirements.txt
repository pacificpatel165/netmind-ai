requests==2.32.3
# chromadb pulled in only for diagnose_and_propose.py, which imports
# diagnosis-assistant/assistant.py (retrieval_client.py needs it) --
# propose.py itself still only needs requests. Pin matches
# diagnosis-assistant/requirements.txt and intelligence/retrieval-index's
# server image exactly -- see retrieval-index/README.md's "known gotcha"
# for why the client/server pin has to stay in lockstep.
chromadb==0.5.23
