# NetMind AI — backlog: deferred decisions & installation index

Two things live in this file, kept separate on purpose:

1. **Deferred decisions** — every point where we deliberately chose
   "smallest thing that works now" over "the more complete answer,"
   with why, and what "done" looks like when we come back to it. The
   component map and build order in `PROGRESS_LOG.md` tell you *what's
   next*; this file tells you *what we already looked at and set
   aside*, so nothing gets silently forgotten between sessions.
2. **Installation index** — a one-page map of everything installed so
   far and where the actual steps live, so "how do I rebuild this
   environment" never means re-reading every session log.

**How to use this file:** when a design discussion produces a "not now,
but note it" decision, add a row here in the same session, dated, with
a pointer to the `PROGRESS_LOG.md` entry it came from. When an item
gets picked up and resolved, move its row to **Resolved** below rather
than deleting it — the record of "we knew about this and chose when to
fix it" is worth keeping.

---

## Open / deferred

| # | Topic | Component | Why deferred | What "done" looks like | Noted |
|---|---|---|---|---|---|
| 1 | Kafka buffer between gnmic and Prometheus | #3 metrics store | Direct scrape proves the pipeline with one less moving part; Kafka is real architecture but adds a third component before Prometheus itself was even confirmed working | gnmic publishes to a Kafka topic, a consumer/exporter feeds Prometheus from it — worth doing once a *second* consumer of the same telemetry stream exists (e.g. #6 retrieval index also wanting it), not before | 2026-09-10, log (9) |
| 2 | Prometheus retention & downsampling policy | #3 metrics store | Roadmap explicitly names this as a learning goal, not a wiring task — there isn't enough real accumulated data yet to reason about the trade-offs concretely | A deliberate retention policy (short local retention + a downsampling or remote-write strategy) chosen *because* of what real data volume/query patterns turned out to need, not chosen in the abstract | 2026-09-10, log (9) |
| 3 | Prometheus persistent storage | #3 metrics store | A bind-mounted `/prometheus` would hit the same container-user (`nobody`) vs host-dir-owner (`clab`) permission mismatch that broke gnmic's file output initially — not worth solving before Prometheus itself is proven | Data survives `clab destroy`/`clab deploy` cycles; needs either a fixed-ownership bind mount or a named Docker volume instead of a host bind | 2026-09-10, log (9) |
| 4 | Kubernetes (k3s) + Cilium underlay | phase 2 / future component | Deliberately sequenced after the telemetry pipeline (#2–#4) is solid, so K8s learning doesn't compete with getting the pipeline proven | k3s running natively inside the `Containerlab` distro (not Docker Desktop's K8s toggle — same isolation problem as before), Cilium as CNI, attached to the `e1-2` interface already reserved on `srl1`/`srl2` | 2026-09-10, log (4) |
| 5 | Grow the lab past 2 nodes (leaf-spine) | #1 network lab | Smallest topology that proves the pipeline came first; topology complexity deferred until there's a real reason to add it (e.g. testing telemetry at scale, or the K8s-underlay attachment) | A second `.clab.yml` stage (kept alongside `netmind-2node.clab.yml`, not overwriting it) with 2 leaf + 2 spine nodes | 2026-09-10, log (2) |
| 6 | Pin container image tags | #2, #3, #4 | `gnmic`, `prom/prometheus`, and now `grafana/grafana` all currently track `:latest` in the topology file, written without a chance to confirm the resolved version first | All three pinned to the exact tag confirmed working, so an upstream release can't silently change lab behavior between sessions | 2026-09-10, log (6, 9, 12) |
| 12 | Grafana lab-only auth posture | #4 dashboards | Anonymous viewer access + a hardcoded admin password in the topology file are fine for a single-user local lab reachable only on `localhost`, not a pattern to carry forward once anything here is ever exposed beyond that | Revisit if/when Grafana (or anything else in this stack) is ever reachable from outside the local machine — swap to real auth, stop committing the admin password in plain YAML | 2026-09-10, log (12) |
| 8 | `systemd` user-session warning on distro boot | tooling | `wsl: Failed to start the systemd user session for 'clab'` appeared on first boot; didn't block anything verified so far, so not investigated | Only worth digging into if something that specifically needs a systemd *user* service (not the root-level services containerlab/Docker CE use) starts misbehaving | 2026-09-10, log (2) |
| 9 | Containerlab distro's baked-in SSH key | tooling / security hygiene | `id_ecdsa` ships identical inside every download of the `wsl-containerlab` image — fine for its original purpose (`ssh clab@localhost`), not something to trust for anything else | A personal `id_ed25519` was already generated and registered on GitHub for git push (steps: `docs/setup/01-network-lab-environment.md` §10) — this row is a standing reminder not to reuse `id_ecdsa` for anything sensitive later, not an open task | 2026-09-10, log (5, 6) — mitigated, not fully "resolved" since the shared key still exists in the distro |
| 11 | Components #7–#11 | diagnosis assistant, remediation proposal, security gate, config-push executor, IaC/environment provisioning | Not started — next in build order after #6 (retrieval index) | Full descriptions live in `PROGRESS_LOG.md`'s Component map; not duplicated here on purpose, so there's one place to check for "what's actually left" | ongoing |
| 13 | Anomaly z-score threshold (3.0) unvalidated | #5 anomaly detection | `ZSCORE_THRESHOLD` was set to a conventional statistics default, not tuned against any real anomaly on this lab — there isn't a known-bad event yet to calibrate against | Threshold adjusted (or made per-metric rather than global) once a real anomaly — even a deliberately triggered one, like manually flapping a link — has been observed and the resulting z-score checked against what actually happened | 2026-09-10, log (14) |
| 14 | Admin/oper state anomaly detection (state flaps) | #5 anomaly detection | Stage 1 deliberately scoped to interface counters only (traffic/errors/discards/transitions); state-change detection is a simpler, more binary signal saved for a deliberate stage 2 rather than folded in on day one | A second watched signal in `detector.py` (or a second detector) flagging unexpected admin/oper state transitions, once counters-based detection is itself validated | 2026-09-10, log (14) |
| 15 | Anomaly detector image tag pinning | #5 anomaly detection | `netmind-anomaly-detector:latest` is a locally built tag, not pulled from a registry — no upstream-drift risk like `gnmic`/`prometheus`/`grafana`'s `:latest`, but still worth a real version scheme once this component stabilizes | A version tag bumped deliberately on real changes (e.g. `netmind-anomaly-detector:0.1.0`) instead of every rebuild silently overwriting `:latest` | 2026-09-10, log (14) |
| 16 | Chroma persistent storage | #6 retrieval index | Same class of deferral as Prometheus (item 3) — a bind-mounted data dir risks the same container-user-vs-host-owner permission mismatch, not worth solving before the retrieval pattern itself is proven | Ingested data survives `clab destroy`/`clab deploy`; needs a fixed-ownership bind mount or named Docker volume, same fix shape as item 3 | 2026-09-10, log (16) |
| 17 | Automatic/scheduled re-ingestion | #6 retrieval index | Stage 1 deliberately manual (`docker run ... ingest.py`) — docs change every session right now, so an automatic watcher would be re-triggering constantly for no benefit yet | A scheduled or file-watch-triggered re-ingest once the docs stabilize enough that "stale index" becomes a real risk rather than a non-issue | 2026-09-10, log (16) |
| 18 | Retrieval quality beyond one ad hoc query | #6 retrieval index | `query.py` proves retrieval *works*, not that it's *good* — no eval set, no relevance grading yet | A small set of question → expected-source pairs checked automatically, once component #7 (diagnosis assistant) actually depends on retrieval quality being good, not just present | 2026-09-10, log (16) |
| 19 | ONNX embedding model re-downloaded every `docker run` | #6 retrieval index | Chroma's default embedding function caches the ~79MB MiniLM ONNX model under `/root/.cache/chroma/` inside the container, but `ingest.py`/`query.py` run via `docker run --rm` — the cache dies with the container, so every invocation re-downloads it (~40s, observed on first real run) | A named Docker volume or bind mount for `/root/.cache/chroma`, shared across `ingest.py`/`query.py` runs — worth it once retrieval is called often enough for the repeated download to actually matter | 2026-09-10, log (17) |
| 20 | `Containerlab` WSL2 distro doesn't auto-start on Windows boot | tooling | `lab-up.sh`/`docker update --restart unless-stopped` only bring containers back once the Docker daemon inside the `Containerlab` distro is running — nothing currently starts that distro automatically when Windows boots, so a laptop restart still needs at least one manual `wsl -d Containerlab` + `lab-up.sh` | Windows Task Scheduler entry (or WSL2's own boot-launch config) that starts the `Containerlab` distro at logon, so `lab-up.sh` alone is enough after any restart — not investigated yet, low priority since the lab isn't meant to run unattended | 2026-09-11, log (18) |
| 23 | LLM-generated PromQL fallback: syntactic validation only, no retry | #7 diagnosis assistant | `router.py`'s fallback path checks a generated query runs successfully against Prometheus, not that it's semantically the *right* query for the question; a rejected query is reported as failed rather than retried/repaired. Deliberately narrow for a stage-1 fallback path that hasn't been exercised with a real off-template question yet | A repair loop (feed the error back to the model, ask again) and/or a semantic check, once real usage shows the syntactic-only check isn't enough | 2026-09-13, log (24) |
| 24 | Component #7 not yet run against the live lab | #7 diagnosis assistant | Built and reviewed (six files, compiles cleanly) but no confirmed execution against the real Ollama/Prometheus/Chroma stack yet | A real question run through `assistant.py` with retrieval, metrics, and citations all showing up correctly in the answer — the actual exit criterion for this component's stage 1 | 2026-09-13, log (24) |
| 26 | Grafana raw-metric panels' `{{name}}` legend references render blank | #4 dashboards | Cosmetic fallout of the same label-key bug — panels don't filter on `name` so the underlying data is correct, only the legend text is affected; not worth rewriting every panel's `legendFormat` until this is confirmed to actually matter day-to-day | Panel `legendFormat` strings swapped from `{{name}}` to `{{interface_name}}`, once someone's actually annoyed by blank legends rather than as a standalone task | 2026-09-13, log (26) |

## Resolved

| # | Topic | Component | Resolved | Notes |
|---|---|---|---|---|
| 10 | gnmic → Prometheus metric names unverified | #3 metrics store | 2026-09-10, log (11) | Confirmed working end-to-end against real scrape output — Prometheus Status→Targets shows `netmind-gnmic` `UP` and `netmind_`-prefixed series return for both nodes. Component #3 stage 1 closed out. |
| 7 | Install `rsync` in the `Containerlab` distro | tooling | 2026-09-11 | `sudo apt install rsync` confirmed done — `sync-to-lab.sh` has been running plain `rsync -a --delete` (no `cp` fallback) successfully across multiple sessions, including the `lab-up.sh` runs on 2026-09-11. Steps captured in `docs/setup/01-network-lab-environment.md` §9. |
| 21 | Local model benchmark (Ollama, size/latency on this machine) | #7 diagnosis assistant | 2026-09-11, log (22) | `llama3.2:3b` vs. `llama3.1:8b` benchmarked with a representative prompt on this machine, lab running. 8B won on correctness (3B misstated the Kafka decision; 8B got it right with proper citations, twice) despite ~2.3x slower (67s vs 29s). Real memory constraint found (8B resident uses ~5GB inside an 11GiB WSL2 VM) and resolved: `keep_alive: 0` per API request (not a CLI env var, which doesn't reach an already-running `ollama serve`) reliably frees it after each use. Decision: Llama 3.1 8B. Full detail in `docs/setup/07-diagnosis-assistant.md` §5. |
| 25 | `interface_name` label-key fix deployed and verified | #5 anomaly detection, #7 diagnosis assistant | 2026-09-13, log (27) | Anomaly-detector image rebuilt and redeployed; `python3 -c "import router; router.resolve_metrics(...)"` run directly (bypassing Ollama) against the live lab returned `netmind_anomaly_score{interface="ethernet-1/1", ...}` with real per-stat z-scores (e.g. `in_octets = -0.267`) instead of `interface="unknown"`. Confirms the fix from log (26) is live, not just committed. Component #5's stage 1 closeout (log 15) is re-affirmed correct now that this landed. |
| 22 | Ollama placement (host vs. containerized) and local-vs-online provider abstraction | #7 diagnosis assistant | 2026-09-13, log (23) | Both decided: Ollama stays host-installed (not containerized) — avoids re-downloading the ~4.9GB model on every `clab destroy`/`deploy` and avoids adding Docker overhead on top of an already memory-tight WSL2 VM. Provider scope stays local-only — 8B already proved correct with proper citations, so there's no concrete gap a Groq/Gemini abstraction would close yet; building one speculatively would break this project's pattern of proving something's needed before building it. Revisit either if a concrete reason shows up (GPU passthrough, a real correctness gap). Full detail in `docs/setup/07-diagnosis-assistant.md`. |

## Installation index

What's been installed, where, and which doc has the actual steps —
use this to find the right file fast rather than re-reading every
session log.

| Installed | Where | Steps live in | Notes |
|---|---|---|---|
| WSL2 update (≥ 2.4.4) | Windows host | `docs/setup/01-network-lab-environment.md` §2 | one-time per machine |
| `wsl-containerlab` distro | Windows host (as a WSL2 distro) | `docs/setup/01-network-lab-environment.md` §3 | official `srl-labs/wsl-containerlab` image |
| Docker Desktop WSL integration turned OFF for `Containerlab` | Windows host (Docker Desktop settings) | `docs/setup/01-network-lab-environment.md` §4 | the step that actually prevents the kernel-namespace conflict |
| Docker CE + containerlab | `Containerlab` distro | `docs/setup/01-network-lab-environment.md` §5 | one combined setup script |
| `gnmic` CLI | `Containerlab` distro | `docs/setup/01-network-lab-environment.md` §8 | separate from containerlab itself |
| `rsync` | `Containerlab` distro | `docs/setup/01-network-lab-environment.md` §9 | `sync-to-lab.sh` depends on it |
| Personal SSH key for GitHub push | `Containerlab` distro | `docs/setup/01-network-lab-environment.md` §10 | needed because the distro's baked-in key isn't yours |
| `srl1` / `srl2` (Nokia SR Linux) | Docker containers, inside `Containerlab` distro | `lab/README.md` | stood up via `clab deploy`, no separate host install |
| `gnmic` collector container | same | `telemetry/README.md` | stood up via `clab deploy` |
| Prometheus container | same | `metrics/README.md` | stood up via `clab deploy`; genuinely no host-level install step — it only exists as a container |
| Grafana container | same | `grafana/README.md` | stood up via `clab deploy`; datasource + dashboard provisioned as code |
| Anomaly detector container | same | `intelligence/anomaly-detection/README.md` | **build the image locally first** (`docker build -t netmind-anomaly-detector:latest .`) — unlike the others, there's no public image to pull |
| Chroma container | same | `intelligence/retrieval-index/README.md` | stood up via `clab deploy`; genuinely no host-level install step — it only exists as a container |
| Retrieval ingest/query tools | same, run on demand (not a topology node) | `intelligence/retrieval-index/README.md` | **build the image locally first** (`docker build -t netmind-retrieval-tools:latest .`), then `docker run` against the `netmind-mgmt` network |
| `scripts/lab-up.sh` / `scripts/lab-down.sh` | `Containerlab` distro | `lab/README.md` §"Starting and stopping the lab" | single-command wrappers around sync + `clab deploy`/`destroy`, plus a fix for containers not restarting after a laptop reboot |
| `zstd` | `Containerlab` distro | `docs/setup/07-diagnosis-assistant.md` §1 | required by the Ollama install script for extraction; not present in the base distro image |
| Ollama + `llama3.1:8b` | `Containerlab` distro (host-installed, by design — see row 22) | `docs/setup/07-diagnosis-assistant.md` | starts its own background `ollama serve` automatically — don't also run it manually, port 11434 will already be bound |
| Diagnosis assistant (`assistant.py` and friends) | same, run on demand (host-level Python, not a topology node — see row 22) | `intelligence/diagnosis-assistant/README.md` | `pip install -r requirements.txt` then `python assistant.py "<question>"`; not yet run against the live lab (row 24) |
